{
  "name": "pi-handoff",
  "version": "0.3.2",
  "type": "module",
  "description": "Self-maintaining HANDOFF.md per working directory for pi, stored under ~/.pi/agent/pi-handoff.",
  "license": "MIT",
  "keywords": ["pi-package", "pi", "handoff"],
  "pi": {
    "extensions": ["./index.ts"],
    "skills": ["./skills"]
  },
  "peerDependencies": {
    "@earendil-works/pi-ai": "*",
    "@earendil-works/pi-coding-agent": "*"
  },
  "//peerDependenciesMeta": "Optional so `npm install` does NOT pull a second copy of the pi runtime into the extension clone. Imports are resolved at runtime by pi (jiti) from the installed pi, never from here.",
  "peerDependenciesMeta": {
    "@earendil-works/pi-ai": { "optional": true },
    "@earendil-works/pi-coding-agent": { "optional": true }
  },
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^24.0.0"
  }
}
