{
  "name": "pi-handoff",
  "version": "0.10.0",
  "type": "module",
  "description": "Self-maintaining handoff.md per working directory + git branch for pi, stored under ~/.agent/agent-handoff.",
  "license": "MIT",
  "keywords": [
    "pi-package",
    "pi",
    "handoff"
  ],
  "pi": {
    "extensions": [
      "./index.ts"
    ],
    "skills": [
      "./skills"
    ]
  },
  "peerDependencies": {
    "@earendil-works/pi-ai": "*",
    "@earendil-works/pi-coding-agent": "*"
  },
  "//peerDependenciesMeta": "Optional so `npm install` does NOT pull a second copy of the pi runtime into the extension clone. Imports are resolved at runtime by pi (jiti) from the installed pi, never from here.",
  "peerDependenciesMeta": {
    "@earendil-works/pi-ai": {
      "optional": true
    },
    "@earendil-works/pi-coding-agent": {
      "optional": true
    }
  },
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "typebox": "^1.3.0",
    "typescript": "^5.9.0"
  }
}
